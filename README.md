Inputs are placed in /inputs and should be named as ```01.txt``` up to ```12.txt```

The problems are solved without the use of LLMs or hints.

Focus has been on efficient STL usage and testing out some C++23 features.
